name: Deploy Backend (Dev)

on:
  workflow_dispatch: {}

concurrency:
  group: deploy-backend-dev-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      ADMIN_SECRET: ${{ secrets.JCOURSE_ADMIN_SECRET }}
      CREDIT_API_BASE: ${{ secrets.CREDIT_API_BASE }}
      VITE_CREDIT_API_BASE: ${{ secrets.VITE_CREDIT_API_BASE }}
      CREDIT_JCOURSE_SECRET: ${{ secrets.CREDIT_JCOURSE_SECRET }}
      JCOURSE_INTEGRATION_SECRET: ${{ secrets.JCOURSE_INTEGRATION_SECRET }}
      TURNSTILE_SECRET_KEY: ${{ secrets.TURNSTILE_SECRET_KEY }}
      TURNSTILE_SITEVERIFY_URL: ${{ secrets.TURNSTILE_SITEVERIFY_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install deps
        run: npm install

      - name: Ensure backend secrets (env.dev)
        run: |
          set -euo pipefail
          if [ -z "$ADMIN_SECRET" ]; then
            echo "Missing secrets.JCOURSE_ADMIN_SECRET (used as backend ADMIN_SECRET)" >&2
            exit 1
          fi
          echo "$ADMIN_SECRET" | npx --yes wrangler@4 versions secret put ADMIN_SECRET --env dev
          BASE="${CREDIT_API_BASE:-${VITE_CREDIT_API_BASE:-}}"
          SECRET="${CREDIT_JCOURSE_SECRET:-${JCOURSE_INTEGRATION_SECRET:-}}"
          if [ -n "${BASE:-}" ]; then
            echo "$BASE" | npx --yes wrangler@4 versions secret put CREDIT_API_BASE --env dev
            echo "$BASE" | npx --yes wrangler@4 versions secret put VITE_CREDIT_API_BASE --env dev
          fi
          if [ -n "${SECRET:-}" ]; then
            echo "$SECRET" | npx --yes wrangler@4 versions secret put CREDIT_JCOURSE_SECRET --env dev
            echo "$SECRET" | npx --yes wrangler@4 versions secret put JCOURSE_INTEGRATION_SECRET --env dev
          fi

          if [ -n "${TURNSTILE_SECRET_KEY:-}" ]; then
            echo "$TURNSTILE_SECRET_KEY" | npx --yes wrangler@4 versions secret put TURNSTILE_SECRET_KEY --env dev
          fi

          if [ -n "${TURNSTILE_SITEVERIFY_URL:-}" ]; then
            echo "$TURNSTILE_SITEVERIFY_URL" | npx --yes wrangler@4 versions secret put TURNSTILE_SITEVERIFY_URL --env dev
          fi

      - name: Deploy Worker (env.dev)
        run: npm run deploy -- --env dev
