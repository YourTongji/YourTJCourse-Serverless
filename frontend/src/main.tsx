import React from 'react'
import ReactDOM from 'react-dom/client'
import ForumRedirectPage from './pages/ForumRedirectPage'
import './styles/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ForumRedirectPage />
  </React.StrictMode>
)
